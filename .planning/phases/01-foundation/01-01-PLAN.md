---
phase: 01-foundation
type: execute
---

<objective>
Create the Lab Link project foundation with GSD planning memory, standalone git setup, npm TypeScript tooling, CI skeleton, and a first runnable CLI shape.
Purpose: Establish enough structure that later PRD features can be added in layers without drifting from the product vision.
Output: Planning docs, package/tooling files, source directories, first CLI/app files, verification scripts.
</objective>

<context>
@LABLINK_PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Repository and planning foundation</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/RALPH_LOOP.md</files>
  <action>Create persistent planning memory based on user decisions, PRD requirements, GSD phase structure, and Ralph-loop iteration rules.</action>
  <verify>Get-ChildItem .planning shows PROJECT.md, ROADMAP.md, STATE.md, RALPH_LOOP.md, and phase plan files.</verify>
  <done>Planning files define current phase, completion promise, roadmap, and next actions.</done>
</task>

<task type="auto">
  <name>Task 2: npm TypeScript CLI scaffold</name>
  <files>package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, eslint.config.js, .gitignore, src/cli.ts, src/app.tsx</files>
  <action>Create an ESM Node 20 TypeScript CLI package using npm scripts from the PRD. Include commander entry point and Ink app shell. Keep dependencies configurable and avoid UI logic in the CLI entry file.</action>
  <verify>npm scripts exist for build, dev, test, lint, typecheck, demo.</verify>
  <done>`lablink` binary points to dist CLI, source compiles in principle, and app launch path is defined.</done>
</task>

<task type="auto">
  <name>Task 3: CI and development docs</name>
  <files>.github/workflows/ci.yml, README.md, docs/IMPLEMENTATION_STRATEGY.md</files>
  <action>Add CI and a concise implementation strategy that explains layers, extension points, OAuth/AI approach, and demo workflow.</action>
  <verify>CI workflow runs npm ci, typecheck, test, and build on push/PR.</verify>
  <done>New developers can understand how to run and extend the project.</done>
</task>
</tasks>

<verification>
- git status shows only intentional Lab Link files.
- npm package metadata matches the PRD.
- Planning state is updated after implementation.
</verification>

<success_criteria>
- Standalone git repo configured.
- Planning docs committed.
- npm scaffold committed.
- First validation attempt recorded.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` with files changed, verification results, risks, and next plan.
</output>
