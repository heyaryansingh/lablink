---
phase: 01-foundation
type: execute
---

<objective>
Stabilize the bootstrap runtime interaction model and make the demo feel like a real terminal product.
Purpose: remove full-screen blink behavior, make arrow-key navigation usable, preserve selection state, and expose a more configurable runtime surface in the product docs.
Output: smoother demo redraws, working arrow-key navigation, customizable UI metadata in the store, updated PRD/gap notes, and a commit.
</objective>

<context>
@bin/lablink.mjs
@docs/product/PRD_V2_2.md
@docs/product/CAPABILITY_GAP_V2_2.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Stabilize redraws</name>
  <files>bin/lablink.mjs</files>
  <action>Switch the interactive runtime from full-screen erase/repaint to stable frame redraws with preserved cursor state and alternate-screen handling.</action>
  <verify>Interactive redraw path no longer clears the whole screen on every keypress.</verify>
  <done>Demo feels smoother and less choppy during navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Add arrow-key navigation</name>
  <files>bin/lablink.mjs</files>
  <action>Make left/right cycle top-level views and make up/down usable for list-based sections and command-center focus.</action>
  <verify>Arrow keys move around the demo meaningfully in command, today, projects, meetings, and AI review views.</verify>
  <done>Navigation contract works in both number-key and arrow-key form.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh product docs</name>
  <files>docs/product/PRD_V2_2.md, docs/product/CAPABILITY_GAP_V2_2.md, .planning/STATE.md</files>
  <action>Capture the customization model and current demo UX improvements in the v2.2 spec and gap matrix.</action>
  <verify>Docs mention the stable interaction model and customization surface.</verify>
  <done>Product definition reflects the actual runtime behavior.</done>
</task>
</tasks>

<verification>
- `git diff --check`
- `npm.cmd run validate`
- Manual demo navigation using number keys, left/right arrows, and up/down arrows.
</verification>

<success_criteria>
- Demo redraw is smoother.
- Arrow keys work in the runtime.
- UI customization surface is documented.
- Commit created.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md`.
</output>

