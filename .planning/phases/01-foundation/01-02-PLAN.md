---
phase: 01-foundation
type: execute
---

<objective>
Make Lab Link launchable through npm with a polished zero-dependency bootstrap runtime.
Purpose: Remove the dependency-install blocker from the user experience and establish a real product shell that can be refined while the full TypeScript/Ink runtime matures.
Output: npm scripts, bin/lablink.mjs, seeded bootstrap store, TUI views, smoke/test/build commands.
</objective>

<context>
@docs/product/PRD_V2.md
@docs/product/PRD_V2_1_LAUNCH_SPEC.md
@docs/product/CAPABILITY_GAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update npm launch path</name>
  <files>package.json</files>
  <action>Make npm install dependency-free and make npm scripts call the bootstrap runtime directly. Preserve TypeScript/Ink source as architecture track, but do not require it for launch.</action>
  <verify>npm.cmd install completes and package-lock.json is created.</verify>
  <done>npm install no longer blocks on external dependency resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Build bootstrap runtime</name>
  <files>bin/lablink.mjs</files>
  <action>Implement a polished Node built-in TUI/runtime with demo data, command center, today, projects, meetings, AI review, settings, search, meeting import, smoke/test/build commands.</action>
  <verify>npm.cmd run smoke renders a deterministic dashboard snapshot.</verify>
  <done>Runtime is useful and professional without external packages.</done>
</task>

<task type="auto">
  <name>Task 3: Validate and record progress</name>
  <files>.planning/STATE.md, .planning/RALPH_LOOP.md, .planning/phases/01-foundation/01-02-SUMMARY.md</files>
  <action>Run npm validation commands, record results, and commit the checkpoint.</action>
  <verify>npm.cmd run validate exits 0.</verify>
  <done>Launch layer is committed with validation results.</done>
</task>
</tasks>

<verification>
- npm.cmd install
- npm.cmd run smoke
- npm.cmd run test
- npm.cmd run build
- npm.cmd run validate
- git diff --check
</verification>

<success_criteria>
- Lab Link launches through npm.
- Demo dashboard is polished and deterministic.
- Meeting import creates AI review suggestions.
- Build creates dist/cli.js.
- Commit created.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md`.
</output>
